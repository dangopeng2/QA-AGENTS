# 硬件-第三方 测试规则文档

> 本文档记录第三方硬件设备在 OneKey Account/Hardware 场景下的专项测试规则。  
> 生成第三方硬件相关测试用例时，必须参考本文档。  
> 当前已沉淀：Ledger；待补充：Trezor。  
> 关联需求：`docs/qa/requirements/Account-Ledger设备识别与匹配规则.md`

---

## 1. 适用范围

| 维度 | 范围 |
|-----|------|
| 设备大类 | Ledger、Trezor（待补充） |
| 功能域 | 设备连接、Wallet 添加、Account 添加、设备匹配校验 |
| 连接方式 | USB、BLE（按设备能力生效） |
| 端 | Desktop（后续端能力扩展时同步补充） |
| 链路优先级 | BTC、EVM 为 P0 回归链路 |

---

## 2. Ledger（已沉淀）

> 连接支持网络：BTC / EVM / SOL / TRON。

### 2.1 USB 设备识别、连接与账户规则

#### 2.1.1 识别顺序

1. 设备连接目标按系统识别顺序决定。  
2. 同时存在多台 Ledger 设备时，只允许尝试连接第一台识别设备。  
3. 当前版本不提供“手动切换/指定目标设备”的交互入口。

#### 2.1.2 多设备行为约束

| 场景 | 期望行为 |
|-----|---------|
| USB 插入多台设备 | 连接列表可展示多台已接入设备 |
| 选择任一连接项 | 实际通讯统一落到第一识别设备（首设备优先） |
| 首设备不可用 | 给出可理解错误提示，不静默切换到后续设备 |
| 会话中设备变化 | 需要重新触发识别，不允许隐式复用旧连接上下文 |

#### 2.1.3 Wallet 维度

| 场景 | 规则 |
|-----|------|
| 同一设备重复添加 wallet | 允许；每次添加可生成新 wallet |
| 设备唯一性 | USB 侧无稳定设备唯一 ID，不以“设备实例”做强限制 |

#### 2.1.4 Account 维度（核心）

1. 校验锚点：以“网络首地址 Hash”作为该网络后续添加的一致性锚点。  
2. 命中锚点网络：再次添加同网络账户时必须校验首地址 Hash 一致性。  
3. 不命中锚点网络：若该网络尚无已添加地址，不做助记词一致性限制。

#### 2.1.5 设备不匹配判定

| 条件 | 结果 |
|-----|------|
| 同网络首地址 Hash 一致 | 允许继续添加 |
| 同网络首地址 Hash 不一致 | 拦截并提示“设备不匹配” |
| 同助记词不同 Ledger 设备 | 允许继续（校验通过） |
| 不同助记词设备且网络已有锚点 | 拦截并提示“设备不匹配” |

### 2.2 BLE 规则

#### 2.2.1 设备标识与聚合

| 场景 | 规则 |
|-----|------|
| 识别设备 | 使用蓝牙标识进行设备识别 |
| 添加 wallet 时蓝牙标识可匹配 | 进入地址匹配判断流程 |
| 添加 wallet 时蓝牙标识不可匹配 | 创建新 wallet |

#### 2.2.2 匹配分支

| 条件 | 结果 |
|-----|------|
| 蓝牙可匹配 且 任一网络地址可匹配 | 合并到已有 wallet |
| 蓝牙可匹配 但 全部网络地址不可匹配 | 创建新 wallet |

### 2.3 账户交互验证（四链）

#### 2.3.1 验证最小集合（P0）

| 类别 | 用例要求 |
|-----|---------|
| BTC | 创建地址、签名、转账流程可达 |
| EVM | 创建地址、签名、转账流程可达 |
| SOL | 创建地址、签名、转账流程可达 |
| TRON | 创建地址、签名、转账流程可达 |
| 不匹配分支 | 在“网络有锚点”前提下可稳定触发“设备不匹配” |

### 2.4 测试设计强制点

1. 必测“网络已建立锚点”和“网络未建立锚点”两类前置状态。  
2. 必测“同助记词跨设备”与“不同助记词跨设备”两类对照样本。  
3. 必测多设备并存时的首设备优先行为。  
4. 必测 BLE 聚合与新建 wallet 两条分支。  
5. 错误提示必须验证文案语义（设备不匹配）与触发时机。

### 2.5 风险清单

- 多设备插拔时序导致“首设备识别”不稳定。  
- 锚点网络与新网络切换时，校验上下文串链。  
- BLE 聚合条件被误用为放行条件，造成错误归并。  
- 四链交互（BTC/EVM/SOL/TRON）中任一链的创建地址/签名/转账流程出现兼容性回退。

---

## 3. Trezor（待补充）

> 状态：占位中，待需求与联调信息补齐后补全规则。

### 3.1 待补充项清单

| 维度 | 待补充内容 |
|-----|-----------|
| 连接能力 | 支持端、支持连接方式（USB/BLE/其他） |
| 识别策略 | 多设备识别顺序、是否支持手动指定设备 |
| wallet/account 规则 | 重复添加策略、设备唯一标识策略 |
| 匹配规则 | 地址匹配口径、设备不匹配提示条件 |
| 回归范围 | P0 链路（BTC/EVM 等）与签名回归要点 |
| 风险项 | 设备固件兼容、连接稳定性、跨端一致性 |

### 3.2 用例设计约束（预留）

1. Trezor 规则未补齐前，不产出“已通过规范审查”的正式专项用例。  
2. 如需临时验证，测试计划中必须标注“探索性验证（非规则完备）”。

---

## 4. 用例产出规范（第三方硬件专项）

- 用例目录：`docs/qa/testcases/cases/hardware/`。  
- 文件命名：`YYYY-MM-DD_Hardware-第三方硬件-<设备>-<主题>.md`。  
- 优先级：
  - P0：设备匹配拦截、BTC/EVM 主流程、多设备首选逻辑
  - P1：提示文案、重复添加一致性
  - P2：低频组合与异常恢复细节

---

## 5. 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-05-18 | v1.1 | 文档重构为“硬件-第三方”规则，现有内容归档到 Ledger 大类，新增 Trezor 待补充大类 |
| 2026-05-18 | v1.0 | 根据 OK-53772 新增 Ledger USB/BLE 识别与匹配专项规则 |
